find_package(GTest REQUIRED)

add_subdirectory(common_utils)

add_executable(test_opencv_yaml_parse test_opencv_yaml_parse.cpp)

target_link_libraries(test_opencv_yaml_parse PRIVATE GTest::GTest GTest::Main common_utils_library)
